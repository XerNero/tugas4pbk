import { createApp } from 'vue'; // Mengimpor createApp dari Vue
import App from './App.vue'; // Mengimpor komponen utama App

// Membuat instance aplikasi Vue dan mount ke elemen dengan id 'app'
createApp(App).mount('#app');
